Used ResNet pre-trained model to generate different layers of image, and used transformer to classify the image according to the layer generated by ResNet. To use it download the python file and run it. Models will start to download and in the "image_path" add your desired image path to generate captions.
